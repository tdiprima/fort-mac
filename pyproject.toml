[project]
name = "mac-update"
version = "0.1.0"
description = "macOS maintenance automation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.3",
    "python-dotenv>=1.2.2",
    "schedule>=1.2.2",
]
